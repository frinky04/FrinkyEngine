# RaylibLogger

Namespace: FrinkyEngine.Core.Rendering

Redirects Raylib's native trace log output to [FrinkyLog](./frinkyengine.core.rendering.frinkylog).

```csharp
public static class RaylibLogger
```

Inheritance [Object](https://docs.microsoft.com/en-us/dotnet/api/system.object) → [RaylibLogger](./frinkyengine.core.rendering.rayliblogger)

## Methods

### **Install()**

Installs the Raylib trace log callback. Call once during initialization.

```csharp
public static void Install()
```
